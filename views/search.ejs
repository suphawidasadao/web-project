<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ค้นหาวงดนตรี</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Bungee&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Kanit', sans-serif;
            background-color: #ffffff;
        }

        .header {
            background: linear-gradient(to bottom, #fba2ce, #ff72a6);
            padding: 12px;
            text-align: center;
        }

        .search-bar {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 10px 0;
        }

        .search-bar input[type="text"] {
            width: 70%;
            padding: 15px;
            border: none;
            border-radius: 20px;
            font-size: 14px;
        }

        .search-bar button {
            background: none;
            border: none;
            cursor: pointer;
            margin-left: -40px; /* Adjust the position of the button */
        }

        .search-bar i {
            font-size: 20px;
            color: #888;
        }

        .profile-icon img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
            margin-left: 30px;
        }

        .filter-buttons {
            text-align: center;
            margin-bottom: 20px;
        }

        .filter-buttons button {
            background-color: white;
            color: #ff6392;
            border: 1px solid #ff6392;
            padding: 10px 20px;
            border-radius: 20px;
            margin: 0 10px;
            cursor: pointer;
            font-size: 14px;
            transition: background-color 0.3s ease;
        }

        .filter-buttons button:hover {
            background-color: #ff6392;
            color: white;
        }

        .artist-list {
            background-color: white;
            margin: 0 auto;
            max-width: 800px;
            padding: 20px;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            margin-top: 30px;
        }

        .artist-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #eee;
        }

        .artist-info {
            display: flex;
            align-items: center;
        }

        .artist-info img {
            border-radius: 50%;
            width: 60px;
            height: 60px;
            margin-right: 20px;
        }

        .artist-info h4 {
            margin: 0;
            font-size: 18px;
        }

        .artist-info p {
            margin: 0;
            color: #888;
            font-size: 12px;
        }

        .view-btn {
            background-color: white;
            border: 1px solid #ff6392;
            padding: 10px 20px;
            border-radius: 30px;
            color: #ff6392;
            cursor: pointer;
            font-size: 14px;
            transition: background-color 0.3s ease;
        }

        .view-btn:hover {
            background-color: #ff6392;
            color: white;
        }

        .message {
            color: red;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <!-- Header and Search Bar Section -->
    <div class="header">      
        <form class="search-bar" action="/search" method="GET">
            <img src="pic/iGOMaVIoPv0OnxuUdZA_2-transformed.png" alt="T-POP Billboard" style="width: 80px;">
            <input type="text" name="q" placeholder="ค้นหาศิลปินที่คุณชื่นชอบ" value="<%= typeof searchTerm !== 'undefined' ? searchTerm : '' %>" required>
            <button><i class="fa fa-search"></i></button>
            <div class="profile-icon">
                <a href="/profile">
                    <img src="https://i.pinimg.com/550x/13/d9/7d/13d97d540be78ef119a9e357b76816a1.jpg" alt="Profile Picture">
                </a>
            </div>
        </form>
        <div class="filter-buttons">
            <button type="button" onclick="filterByType('all')">ทั้งหมด</button>
            <button type="button" onclick="filterByType('group')">ศิลปินกลุ่ม</button>
            <button type="button" onclick="filterByType('solo')">ศิลปินเดี่ยว</button>
            <button type="button" onclick="filterByType('duo')">ศิลปินดูโอ้</button>
        </div>
        <% if (typeof message !== 'undefined') { %>
            <p class="message"><%= message %></p>
        <% } %>
    </div>

    <!-- Artist List Section -->
    <div class="artist-list" id="artist-list">
        <% if (bands.length > 0) { %>
            <% bands.forEach(band => { %>
                <div class="artist-item">
                    <div class="artist-info">
                        <img src="<%= band.band_picture %>" alt="<%= band.band_picture %>" onerror="this.onerror=null; this.src='/path/to/default-image.jpg';">
                        <div>
                            <h4><%= band.band_name %></h4>
                            <p>70,023 fans</p>
                        </div>
                    </div>
                    <button class="view-btn" onclick="window.location.href='/band/<%= band.id_bands %>'">เข้าชม</button>
                </div>
                
            <% }); %>
        <% } else if (typeof searchTerm !== 'undefined') { %>
            <p>ไม่พบวงดนตรีที่ตรงกับคำค้นหา "<%= searchTerm %>"</p>
        <% } %>
    </div>

    <script>
        function filterArtists() {
            const input = document.querySelector('.search-bar input[type="text"]').value.toLowerCase();
            const artistList = document.getElementById('artist-list');
            const artistItems = artistList.getElementsByClassName('artist-item');

            Array.from(artistItems).forEach(item => {
                const artistName = item.getElementsByTagName('h4')[0].innerText.toLowerCase();
                item.style.display = artistName.includes(input) ? '' : 'none'; // Show/hide based on input
            });
        }

        function filterByType(type) {
            const artistList = document.getElementById('artist-list');
            const artistItems = artistList.getElementsByClassName('artist-item');

            Array.from(artistItems).forEach(item => {
                if (type === 'all' || item.getAttribute('data-type') === type) {
                    item.style.display = ''; // Show all or matching type
                } else {
                    item.style.display = 'none'; // Hide non-matching type
                }
            });
        }
    </script>
</body>
</html>
