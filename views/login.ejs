<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Lora:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Lora', serif;
        }
    </style>
</head>

<body style="height: 100vh; margin: 0; padding: 0; background: linear-gradient(to bottom, #ff9fdf, #F0569A);">
    <div>
        <img src="/pic/Screenshot 2024-08-09 124651.png" alt="">
    </div>
    <div class="flex justify-center items-center">
        <div class="col-sm-6 rounded py-3">      
            <h1 class="text-6xl text-center text-light pt-8 font-bold">T-POP Hub</h1>
            <form action="/login" method="POST" novalidate>
                <div class="flex flex-col items-center pt-10">
                    <div class="form-group w-full max-w-md">
                        <div class="flex items-center">
                            <span class="absolute ml-3 text-gray-500">
                                <i class="fas fa-envelope"></i>
                            </span>
                            <input type="email" name="user_email" id="_remail" class="form-control rounded-xl pl-10 w-full py-4" placeholder="Email"
                                value="<%= (locals.old_data) ? old_data.user_email : ''; %>" required>
                        </div>
                    </div>
                    <div class="form-group w-full max-w-md">
                        <div class="flex items-center">
                            <span class="absolute ml-3 text-gray-500">
                                <i class="fas fa-lock"></i>
                            </span>
                            <input type="password" name="user_pass" id="_rpass" class="form-control rounded-xl pl-10 w-full py-4" placeholder="Password"
                                required>
                        </div>
                    </div>
                </div>
                <div class="flex flex-col items-center">
                    <% if (locals.register_error) { register_error.forEach(function(error_msg){ %>
                        <div class="alert alert-danger w-full max-w-md" role="alert">
                            <%= error_msg %>
                        </div>
                    <% }); } %>
                
                    <button type="submit" name="action" value="login" class="btn bg-blue-500 mt-4 w-40 max-w-md text-center text-light rounded-xl">Login</button>
                    <button type="button" class="btn btn-secondary mt-2 w-40 max-w-md text-center text-light rounded-xl" 
                            onclick="window.location.href='/register'">Sign Up</button>
                </div>
        </div>
    </div>
</body>

</html>
